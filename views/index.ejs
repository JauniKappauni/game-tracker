<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title%></title>
  </head>
  <body>
    <h2>Games</h2>
    <form method="post" action="/add-game">
      <input type="text" name="link" />
      <input type="submit" />
    </form>
    <% if(games) { %> <% games.forEach(game => { %>
    <a href="<%=game.link%>"><li><%=game.name%></li></a>
    <li><%=game.price%></li>
    <% }) %> <% %> <% } %>
  </body>
</html>
