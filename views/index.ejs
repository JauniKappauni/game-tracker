<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title%></title>
  </head>
  <body>
    <h2>Games</h2>
    <% games.forEach(game => { %>
    <li><%=game.id%></li>
    <li><%=game.name%></li>
    <li><%=game.link%></li>
    <li><%=game.category%></li>
    <% }) %>
  </body>
</html>
